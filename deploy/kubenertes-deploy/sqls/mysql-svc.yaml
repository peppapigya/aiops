apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  labels:
    app: mysql-svc
    component: aiops
  namespace: aiops
spec:
  selector:
    app: mysql
    component: aiops
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: server

